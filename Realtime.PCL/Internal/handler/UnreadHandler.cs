using System;

namespace LeanCloud.Realtime { 
    internal class UnreadHandler : CommandHandler {
        internal UnreadHandler(Connection connection) : base(connection) {
        
        }

        internal override void Handle(GenericCommand command) {
            //throw new NotImplementedException();
        }
    }
}
